# Post-DIV-87 A and independent B resumption decision

Mason approved this resumption and one added execution slot ("approved to proceed") after the
verified DIV-87 release. A was dispatched once after preflight; B still requires selection of A's actual published
proposal. Preflight found a completed unrelated automation with a live provider sandbox; Mason
separately authorized terminating that exact sandbox, and exit 137 plus an empty app listing were
verified before enabling A. A was then dispatched once: [A3 closeout](div77-a3-closeout-2026-09-17.md). Actual DIV-88
publication and issue deduplication passed; replay duplicated the native report. B remains stopped
for [actual proposal selection](div77-div88-b-decision-2026-09-17.md). No source-tool smoke or completed tests need repeating.
Linear `5759100c-3cde-439a-b900-fc5c058d0767` is now deployed with publication false/implementation;
control plane remains `2ed9a9a5-353d-4e39-a97b-6e8f5192ad8f`.

## Approved decision

Resume the stopped pilot and add one execution slot to the one remaining for replacement A and
later independently approved B. Authorize temporary dev Linear flags for A (`read-only`, publication
`true`), publication `false` for B, and restoration to `implementation` when idle. Review fresh
Linear-only plans before each apply; stop on scope expansion. No Modal/image release, access
expansion or scheduler. B still requires Mason's selection of A's actual published proposal.

## Approved A instruction — executed once

This exact scope was published for A3 and executed once. Retain it as evidence; do not redispatch
it or reuse historical launch helpers/output paths.

> This is a fresh post-DIV-87 stage A of the DIV-77 enforced-investigation pilot, superseding older stopped
> instructions and the failed A2 attempt. Repository mdumas38/background-agents, branch main, expected revision
> 1f7cbe3a0ac004f38f6313977c704ad286ace55c. Enforced investigation profile: Modal, OpenCode,
> openrouter/deepseek/deepseek-v4.1-flash. One worker; no delegation or independent dispatch.
>
> Investigate whether a published follow-up preserves enough source-report and source-revision
> provenance for a fresh independently dispatched read-only worker to validate its findings.
> Trace publication and follow-up prompt assembly in packages/linear-bot/src/follow-ups and the
> dispatch prompt builder. Cite paths and distinguish verified behavior from unknowns. Use only
> source read/glob/grep. Aim for eight calls and five minutes; keep the complete report, including
> any proposal, at or below 5,000 characters.
> No shell/tests/edits, credentials, installed binaries or network tools. Do not retry denied
> operations or seek unrestricted alternatives.
>
> If evidence warrants one next READ-ONLY validation task, include at most ONE openinspect-follow-up
> block using the integration-provided schema, with self-contained evidence, exact source revision,
> scope, acceptance criteria and uncertainties. Prefer a bounded source-only question. Do not
> manufacture a proposal. The proposal block must start immediately with a single `# Title` line.
> Use the exact integration-provided openinspect-follow-up fence; never wrap the entire proposal
> body in an additional Markdown fence. Inner examples may use fences only as the schema allows.
> Preserve every required schema section; honor the stricter 5,000-character complete-report limit.
> Existing work includes DIV-76 normalization, DIV-79 host memory, DIV-83
> historical Modal diagnosis, DIV-84 durable callbacks and repaired DIV-78/DIV-85/DIV-86/DIV-87; do not
> duplicate these issues or propose their implementation. If no new task is warranted, explain why
> and publish no proposal.
>
> Publication is enabled only for A and grants no compute. B requires Mason to select the ACTUAL
> published proposal before separate dispatch. End with DIV77_A_COMPLETE. Operator stop at ten
> minutes from creation or $0.50 observed model spend. No automatic retry after failure.

## Operator acceptance and budget

Verify source, current deployments, no active sessions and no unrelated work exposed to temporary
flags. Preserve the full current instruction, required issue/report and integration publication
contract; optional history may be omitted only through the disclosed DIV-86 fallback. Never force
admission or trim required reports. Use fresh private evidence and a single-attempt launch marker.

One active worker maximum. Aim five minutes; stop at ten minutes from creation or $0.50 observed
model spend. Target $2 including infrastructure reserve is not a hard cap. Record unknown
infrastructure/classifier costs explicitly.

Capture issue/session/message/report links, actual source revision and costs. Verify actual durable
proposal/report/provenance, unassigned Backlog state and no compute from publication. Replay a
successful logical completion once; distinguish publication deduplication from duplicate native
report delivery. Terminate A and confirm provider exit. Failure stops downstream work; no warranted
proposal is valid and grants no substitute run.

Present A's actual task for Mason's selection. Independently dispatch B with investigation profile
and publication off, preserving the required report through normal prompt assembly. Verify context
transfer, completion, no child publication, costs and termination; restore safe flags. DIV-84's
transactional outbox/exactly-once durability remains unimplemented and requires implementation or
explicit acceptance revision before DIV-77 closes. DIV-79 stays separate.


A2's unpublished report and operator-created DIV-87 are not eligible B selections. A malformed
proposal or publication rejection is a failure, never a valid no-proposal outcome. Publication alone
authorizes no compute. Do not launch from this document, post its instruction, change pilot flags,
or invoke historical launch scripts blindly. Resumption and the added slot are approved; all live
preflight checks and B's separate selection gate still apply.
